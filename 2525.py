x, y = input().split()
C = int(input()) #추가 분
A = int(x) # 시
B = int(y) # 분

D = B+C # 분의 합 # 이 식만 처리해서 넘겨주면 끝 

if D >= 60:
    A +=D // 60
    D = D%60

if A >= 24:
    A -=24

print(A, D) 

'''

if D >= 60: #분이 60이거나 넘음?
    if A+(D//60) >= 24:  시가 24를 넘음?
        print(0+(D//60)-1, D%60) 오류 : 0시를 넘어도 -1을 뺐다고 다 맞는 것이 아님
    else:
        print(A+(D//60), D%60)

elif D < 60:
    print(A, D)
시(A)가 24를 넘는 경우 처리 문제:

첫 번째 if 블록에서 if A + (D // 60) >= 24: 조건이 참이면 시(A)가 24를 초과할 때 (0 + (D // 60) - 1)을 시로 출력하고 있습니다. 이 부분이 비논리적입니다.
예를 들어, A + (D // 60)이 24를 넘으면 새 시(A)를 24로 나누어 다시 계산해야 합니다.
분(D)이 60 이상인 경우, 분 계산이 잘못될 수 있음:

코드에서 분(D)이 60 이상인 경우 시(A)에 (D // 60)을 더하고 나머지를 출력하는 부분은 옳지만, 시(A)가 24 이상이 되는 경우를 제대로 처리하지 못할 수 있습니다.
일관된 출력이 아닌 문제 : 시(A)가 24를 넘는 경우와 넘지 않는 경우에 대한 출력이 일관되지 않습니다. 예를 들어, 시(A)가 24 이상이면 시를 0으로 초기화한 후 (D // 60) - 1을 시로 출력하는 논리는 실제 상황을 반영하지 못합니다.

'''